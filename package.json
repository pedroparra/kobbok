{
    "name": "kobbok",
    "private": true,
    "scripts": {
        "start": "node index",
        "server": "node index",
        "lint": "prettier --write .",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "prettier": {
        "tabWidth": 4,
        "printWidth": 130,
        "singleQuote": true
    },
    "dependencies": {
        "ejs": "^3.1.6",
        "express": "^4.17.3",
        "save": "^2.4.0"
    },
    "devDependencies": {
        "prettier": "^2.6.1"
    }
}
